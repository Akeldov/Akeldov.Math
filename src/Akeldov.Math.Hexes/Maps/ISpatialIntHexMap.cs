namespace Akeldov.Math.Hexes
{
    /// <summary>
    /// Provides read-only access to integer values and spatial geometry over a rectangular hex map.
    /// </summary>
    public interface ISpatialIntHexMap : ISpatialHexMap<int>, IIntHexMap
    {
    }
}
