# Edge Cases

Focused edge-case regression tests live next to the API they cover.

Examples include:

- curve projection and intersection cases;
- rectangle membership and normalization;
- contour closure and containment;
- Voronoi empty-cell behavior;
- raster grid coordinate mapping.

Add focused regressions to the existing domain test class when the case belongs to one type.
