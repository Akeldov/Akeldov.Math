# Contour Signed Distance

Contour signed-distance rasterizers sample distance to a closed boundary.

Negative distances are inside the contour. Positive distances are outside.

Use contour signed distance when the boundary itself is the source geometry and you do not need region fill rules across multiple contours.
