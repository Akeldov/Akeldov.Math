# Voronoi Partitioning

Voronoi partitioning compares positioned items against configured sites.

Cost grows with item count and site count. Weighted sites add arithmetic but do not change the conceptual assignment model.

Benchmark when partitioning large item sets repeatedly or inside an interactive loop.
