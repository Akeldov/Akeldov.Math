# Snapshot Tests

Snapshot tests verify visual or rasterized behavior against approved output.

They are useful for:

- curve distance rasters;
- contour and region rasterization;
- Poisson disk visualizations;
- influence field heatmaps;
- culling maps.

When snapshot output changes, inspect the generated image before approving the update.
