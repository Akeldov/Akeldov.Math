# Hexes API Reference

<div data-reference-language="en">

The Hexes API reference documents the public namespaces, types, members, parameters, return
values, and exceptions provided by `Akeldov.Math.Hexes`. Use it when you need the exact contract
of a class or method after choosing an approach in the conceptual documentation.

Use the version selector before relying on a signature: each Reference branch is generated from
that version's assembly and XML documentation.

> [!NOTE]
> All Reference pages except this overview are available in English only because they are
> generated from the API XML documentation.

</div>

<div data-reference-language="ru" hidden>

Справочник API Hexes описывает публичные пространства имён, типы, члены, параметры,
возвращаемые значения и исключения пакета `Akeldov.Math.Hexes`. Используйте его, когда после
выбора подхода в концептуальной документации требуется точный контракт класса или метода.

Перед использованием сигнатуры проверьте выбранную версию: каждая ветка справочника создаётся из
сборки и XML-документации соответствующей версии.

> [!NOTE]
> Все страницы Справочника, кроме этой обзорной, доступны только на английском языке, поскольку
> они генерируются из XML-документации API.

</div>
